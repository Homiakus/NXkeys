name: Apply profile editor and command menu fixes

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/apply-profile-menu-fix.yml'

permissions:
  contents: write

jobs:
  patch-test-commit:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Apply canonical path editor and complete menu rendering
        shell: pwsh
        run: |
          @'
          from pathlib import Path
          import re

          root = Path.cwd()

          def read(path):
              return (root / path).read_text(encoding='utf-8-sig')

          def write(path, content):
              target = root / path
              target.parent.mkdir(parents=True, exist_ok=True)
              target.write_text(content.replace('\r\n', '\n'), encoding='utf-8', newline='\n')

          def replace_once(path, old, new):
              text = read(path)
              count = text.count(old)
              if count != 1:
                  raise RuntimeError(f'{path}: expected one exact match, found {count}: {old[:120]!r}')
              write(path, text.replace(old, new, 1))

          def regex_once(path, pattern, replacement):
              text = read(path)
              updated, count = re.subn(pattern, replacement, text, count=1, flags=re.S)
              if count != 1:
                  raise RuntimeError(f'{path}: expected one regex match, found {count}: {pattern[:120]!r}')
              write(path, updated)

          write('NX2512_HotkeyStudio/Models/EditableCommandPathPolicy.cs', r'''using System;
          using System.Collections.Generic;
          using System.Linq;

          namespace NX2512_HotkeyStudio.Models
          {
              /// <summary>
              /// Canonical editor contract for schema-v6 mnemonic paths. Runtime dispatch uses Path;
              /// SubmenuKey/InputKey are maintained only as a backward-compatible projection.
              /// </summary>
              public static class EditableCommandPathPolicy
              {
                  public static IReadOnlyList<string> EffectivePath(ModuleCommand command)
                  {
                      IReadOnlyList<string> canonical = MnemonicPathGenerator.NormalizePath(command?.Path);
                      if (canonical.Count > 0) return canonical;
                      string submenu = LeaderKeyConfig.NormalizeInputKey(command?.SubmenuKey);
                      string input = LeaderKeyConfig.NormalizeInputKey(command?.InputKey);
                      if (string.IsNullOrWhiteSpace(input)) return Array.Empty<string>();
                      return string.IsNullOrWhiteSpace(submenu)
                          ? new[] { input }
                          : new[] { submenu, input };
                  }

                  public static string FormatPath(IEnumerable<string> path) =>
                      string.Join(" → ", MnemonicPathGenerator.NormalizePath(path));

                  public static string FormatLabels(IEnumerable<string> labels) =>
                      string.Join(" → ", (labels ?? Enumerable.Empty<string>())
                          .Select(value => (value ?? string.Empty).Trim())
                          .Where(value => value.Length > 0));

                  public static List<string> ParsePath(string value)
                  {
                      if (string.IsNullOrWhiteSpace(value)) return new List<string>();
                      string normalized = value.Replace("->", " ").Replace("→", " ").Replace(">", " ")
                          .Replace("/", " ").Replace("\\", " ").Replace(",", " ").Replace(";", " ");
                      string[] parts = normalized.Split((char[])null, StringSplitOptions.RemoveEmptyEntries);
                      IEnumerable<string> raw = parts.Length == 1 && parts[0].Length > 1 && parts[0].All(char.IsLetterOrDigit)
                          ? parts[0].Select(character => character.ToString())
                          : parts;
                      return raw.Select(LeaderKeyConfig.NormalizeInputKey)
                          .Where(token => !string.IsNullOrWhiteSpace(token))
                          .Take(5)
                          .ToList();
                  }

                  public static List<string> ParseLabels(string value, IReadOnlyList<string> path, string commandName)
                  {
                      if (!string.IsNullOrWhiteSpace(value))
                      {
                          string normalized = value.Replace("->", "|").Replace("→", "|").Replace(">", "|").Replace(";", "|");
                          List<string> labels = normalized.Split(new[] { '|' }, StringSplitOptions.RemoveEmptyEntries)
                              .Select(item => item.Trim()).Where(item => item.Length > 0).ToList();
                          if (labels.Count == path.Count) return labels;
                      }
                      return MnemonicPathGenerator.BuildPathLabels(path, commandName);
                  }

                  public static void ApplyEditedPath(ModuleCommand command, string pathText, string labelsText)
                  {
                      if (command == null) return;
                      List<string> path = ParsePath(pathText);
                      IReadOnlyList<string> previous = EffectivePath(command);
                      bool changed = !previous.SequenceEqual(path, StringComparer.OrdinalIgnoreCase);
                      command.Path = path;
                      command.PathLabels = ParseLabels(labelsText, path, command.Command?.Name);
                      if (changed)
                      {
                          command.PathLocked = true;
                          command.PathSource = "user";
                      }
                      command.Aliases ??= new List<List<string>>();
                      command.Aliases = command.Aliases
                          .Select(alias => MnemonicPathGenerator.NormalizePath(alias).ToList())
                          .Where(alias => alias.Count > 0 && !alias.SequenceEqual(path, StringComparer.OrdinalIgnoreCase))
                          .GroupBy(alias => string.Concat(alias), StringComparer.OrdinalIgnoreCase)
                          .Select(group => group.First()).ToList();
                      SyncLegacyFields(command);
                  }

                  public static void Normalize(Config config)
                  {
                      if (config == null) return;
                      foreach (ModuleConfig module in config.Modules ?? new List<ModuleConfig>())
                      {
                          IEnumerable<ModuleCommand> commands = module?.CommandSets?
                              .Where(set => set?.Commands != null).SelectMany(set => set.Commands)
                              .Where(command => command != null) ?? Enumerable.Empty<ModuleCommand>();
                          foreach (ModuleCommand command in commands)
                          {
                              List<string> path = EffectivePath(command).ToList();
                              command.Path = path;
                              if (command.PathLabels == null || command.PathLabels.Count != path.Count)
                                  command.PathLabels = MnemonicPathGenerator.BuildPathLabels(path, command.Command?.Name);
                              SyncLegacyFields(command);
                          }
                      }
                      config.LeaderKey?.RebuildFromModules(config.Modules);
                  }

                  public static List<string> Validate(Config config)
                  {
                      var problems = new List<string>();
                      if (config == null) { problems.Add("Профиль отсутствует."); return problems; }
                      foreach (ModuleConfig module in (config.Modules ?? new List<ModuleConfig>()).Where(value => value != null && value.Enabled))
                      {
                          string prefix = LeaderKeyConfig.NormalizeInputKey(module.LeaderPrefix);
                          if (string.IsNullOrWhiteSpace(prefix)) problems.Add($"{module.Label}: отсутствует leader_prefix");
                          var seen = new List<(string Sequence, string Path, string Command)>();
                          IEnumerable<ModuleCommand> commands = module.CommandSets?
                              .Where(set => set?.Commands != null).SelectMany(set => set.Commands)
                              .Where(command => command != null && command.Enabled) ?? Enumerable.Empty<ModuleCommand>();
                          foreach (ModuleCommand command in commands)
                          {
                              IReadOnlyList<string> path = EffectivePath(command);
                              string pathText = FormatPath(path);
                              string commandName = command.Command?.Name ?? command.Command?.ID ?? "без названия";
                              if (path.Count == 0) problems.Add($"{module.Label}: пустой путь для {commandName}");
                              if (string.IsNullOrWhiteSpace(command.Command?.Name)) problems.Add($"{module.Label}: пустое Command Name для пути {pathText}");
                              if (string.IsNullOrWhiteSpace(command.Command?.ID)) problems.Add($"{module.Label}: пустой BUTTON ID для пути {pathText}");
                              if (path.Count == 0 || string.IsNullOrWhiteSpace(prefix)) continue;
                              string sequence = prefix + string.Concat(path);
                              foreach ((string existingSequence, string existingPath, string existingCommand) in seen)
                              {
                                  if (string.Equals(sequence, existingSequence, StringComparison.OrdinalIgnoreCase))
                                      problems.Add($"{module.Label}: путь {pathText} повторяется у {existingCommand} и {commandName}");
                                  else if (sequence.StartsWith(existingSequence, StringComparison.OrdinalIgnoreCase) ||
                                           existingSequence.StartsWith(sequence, StringComparison.OrdinalIgnoreCase))
                                      problems.Add($"{module.Label}: пути {existingPath} и {pathText} образуют конфликт команда/подменю");
                              }
                              seen.Add((sequence, pathText, commandName));
                          }
                      }
                      return problems.Distinct(StringComparer.OrdinalIgnoreCase).ToList();
                  }

                  public static void SyncLegacyFields(ModuleCommand command)
                  {
                      IReadOnlyList<string> path = MnemonicPathGenerator.NormalizePath(command?.Path);
                      if (command == null) return;
                      command.SubmenuKey = path.Count > 1 ? path[0] : string.Empty;
                      command.InputKey = path.LastOrDefault() ?? string.Empty;
                      command.SubmenuLabel = path.Count > 1 && command.PathLabels != null && command.PathLabels.Count > 0
                          ? command.PathLabels[0]
                          : string.Empty;
                  }
              }
          }
          ''')

          write('NX2512_HotkeyStudio/UI/CommandMenuPolicy.cs', r'''using System;
          using System.Collections.Generic;
          using System.Linq;
          using NX2512_HotkeyStudio.Models;

          namespace NX2512_HotkeyStudio.UI
          {
              public static class CommandMenuPolicy
              {
                  public static string ResolveMenuLabel(string key, IEnumerable<LeaderSequenceItem> values, int prefixDepth)
                  {
                      List<LeaderSequenceItem> group = (values ?? Enumerable.Empty<LeaderSequenceItem>()).Where(item => item != null).ToList();
                      int pathIndex = Math.Max(0, prefixDepth - 1); // Sequence includes the module prefix; PathLabels does not.
                      string label = group.Select(item => item.PathLabels != null && item.PathLabels.Count > pathIndex
                              ? item.PathLabels[pathIndex]
                              : string.Empty)
                          .FirstOrDefault(value => !string.IsNullOrWhiteSpace(value));
                      if (string.IsNullOrWhiteSpace(label) && pathIndex == 0)
                          label = group.Select(item => item.SubmenuLabel).FirstOrDefault(value => !string.IsNullOrWhiteSpace(value));
                      return string.IsNullOrWhiteSpace(label) ? "Подменю " + key : label;
                  }
              }
          }
          ''')

          # Preserve user-owned canonical paths instead of silently reallocating them during Config.Save().
          replace_once('NX2512_HotkeyStudio/Models/MnemonicPathGenerator.cs',
          '''                    MnemonicDefinition definition = ResolveDefinition(command);
                              IReadOnlyList<string> requested = NormalizePath(command.Path);
                              if (requested.Count == 0) requested = NormalizePath(definition?.Path);
                              if (requested.Count == 0) requested = GenerateCandidate(module, command);
                              List<string> canonical = ReserveUnique(requested, command, usedCanonical);
                              command.Path = canonical;

                              if (command.PathLabels.Count != canonical.Count)
                                  command.PathLabels = BuildLabels(canonical, command.Command?.Name);''',
          '''                    MnemonicDefinition definition = ResolveDefinition(command);
                              IReadOnlyList<string> requested = NormalizePath(command.Path);
                              bool locked = command.PathLocked || string.Equals(command.PathSource, "user", StringComparison.OrdinalIgnoreCase);
                              List<string> canonical;
                              if (locked)
                              {
                                  if (requested.Count == 0)
                                      throw new InvalidOperationException("Locked mnemonic path is empty for " + (command.Command?.ID ?? command.Command?.Name));
                                  string lockedKey = string.Concat(requested);
                                  if (usedCanonical.ContainsKey(lockedKey) || CreatesPrefixConflict(lockedKey, usedCanonical.Keys))
                                      throw new InvalidOperationException("Locked mnemonic path conflicts in module " + module.ID + ": " + string.Join(" → ", requested));
                                  canonical = requested.ToList();
                                  usedCanonical[lockedKey] = command;
                              }
                              else
                              {
                                  if (requested.Count == 0) requested = NormalizePath(definition?.Path);
                                  if (requested.Count == 0) requested = GenerateCandidate(module, command);
                                  canonical = ReserveUnique(requested, command, usedCanonical);
                              }
                              command.Path = canonical;

                              if (command.PathLabels.Count != canonical.Count)
                                  command.PathLabels = BuildLabels(canonical, command.Command?.Name);''')

          replace_once('NX2512_HotkeyStudio/Models/MnemonicPathGenerator.cs',
          '        private static List<string> BuildLabels(',
          '''        public static List<string> BuildPathLabels(IReadOnlyList<string> path, string commandName) =>
                      BuildLabels(path ?? Array.Empty<string>(), commandName);

                  private static List<string> BuildLabels(''')

          # The editor now edits the canonical Path/PathLabels contract instead of obsolete two-level fields.
          replace_once('NX2512_HotkeyStudio/UI/HotkeyStudioForm.cs',
          '''            moduleGrid.Columns.Add(new DataGridViewTextBoxColumn { Name = "SubmenuKey", HeaderText = "Menu Key", Width = 72 });
                      moduleGrid.Columns.Add(new DataGridViewTextBoxColumn { Name = "SubmenuLabel", HeaderText = "Menu Label", Width = 140 });
                      moduleGrid.Columns.Add(new DataGridViewTextBoxColumn { Name = "Key", HeaderText = "Key", Width = 54 });''',
          '''            moduleGrid.Columns.Add(new DataGridViewTextBoxColumn { Name = "Path", HeaderText = "Путь", Width = 145 });
                      moduleGrid.Columns.Add(new DataGridViewTextBoxColumn { Name = "PathLabels", HeaderText = "Подсказки пути", Width = 190 });''')

          replace_once('NX2512_HotkeyStudio/UI/HotkeyStudioForm.cs',
          '''                        command.SubmenuKey,
                                  command.SubmenuLabel,
                                  command.InputKey,
                                  string.IsNullOrWhiteSpace(command.IconHint)
                                      ? CommandIconHints.FromCommand(command.Command?.ID, command.Command?.Name, command.SubmenuKey, command.SubmenuLabel)''',
          '''                        EditableCommandPathPolicy.FormatPath(EditableCommandPathPolicy.EffectivePath(command)),
                                  EditableCommandPathPolicy.FormatLabels(command.PathLabels),
                                  string.IsNullOrWhiteSpace(command.IconHint)
                                      ? CommandIconHints.FromCommand(command.Command?.ID, command.Command?.Name,
                                          command.Path?.FirstOrDefault(), command.PathLabels?.FirstOrDefault())''')

          replace_once('NX2512_HotkeyStudio/UI/HotkeyStudioForm.cs',
          '''                command.Enabled = ReadBool(row, "Enabled");
                              command.SubmenuKey = LeaderKeyConfig.NormalizeInputKey(ReadText(row, "SubmenuKey"));
                              command.SubmenuLabel = ReadText(row, "SubmenuLabel").Trim();
                              command.InputKey = LeaderKeyConfig.NormalizeInputKey(ReadText(row, "Key"));
                              command.IconHint = ReadText(row, "Icon").Trim();''',
          '''                command.Enabled = ReadBool(row, "Enabled");
                              EditableCommandPathPolicy.ApplyEditedPath(command, ReadText(row, "Path"), ReadText(row, "PathLabels"));
                              command.IconHint = ReadText(row, "Icon").Trim();''')

          replace_once('NX2512_HotkeyStudio/UI/HotkeyStudioForm.cs',
          '''            try
                      {
                          ValidateEditableCommands();
                          config.Save(configPath);''',
          '''            try
                      {
                          EditableCommandPathPolicy.Normalize(config);
                          ValidateEditableCommands();
                          config.Save(configPath);''')

          regex_once('NX2512_HotkeyStudio/UI/HotkeyStudioForm.cs',
          r'        private void ValidateEditableCommands\(\)\n        \{.*?\n        \}\n\n        private void MarkDirty\(\)',
          '''        private void ValidateEditableCommands()
                  {
                      List<string> problems = EditableCommandPathPolicy.Validate(config);
                      if (problems.Count == 0) return;
                      const int maximumVisible = 60;
                      string omitted = problems.Count > maximumVisible
                          ? $"\\n- … и ещё {problems.Count - maximumVisible} ошибок"
                          : string.Empty;
                      throw new InvalidOperationException("Профиль не сохранён:\\n- " +
                          string.Join("\\n- ", problems.Take(maximumVisible)) + omitted);
                  }

                  private void MarkDirty()''')

          replace_once('NX2512_HotkeyStudio/UI/HotkeyStudioForm.cs',
          '                        .Select(item => new { item.SubmenuKey, item.SubmenuLabel, item.InputKey, item.IconHint, item.Sequence, command_id = item.Command?.ID, command_name = item.Command?.Name }).ToList())',
          '                        .Select(item => new { item.Path, item.PathLabels, item.IconHint, item.Sequence, command_id = item.Command?.ID, command_name = item.Command?.Name }).ToList())')

          # Runtime HUD: resize to the current branch, render every next-key option, and label nested levels correctly.
          replace_once('NX2512_HotkeyStudio/UI/LeaderHudForm.cs',
          '''            targetOpacity = opacity;
                      PositionNearCursor();''',
          '''            targetOpacity = opacity;
                      FitToContent();
                      PositionNearCursor();''')

          replace_once('NX2512_HotkeyStudio/UI/LeaderHudForm.cs',
          '''            confirmationItem = null;
                      timeoutPct = 1.0f;
                      Invalidate();
                  }

                  public void SetSearchMode''',
          '''            confirmationItem = null;
                      timeoutPct = 1.0f;
                      FitToContent();
                      PositionNearCursor();
                      Invalidate();
                  }

                  public void SetSearchMode''')

          replace_once('NX2512_HotkeyStudio/UI/LeaderHudForm.cs',
          '''            currentPrefix = prefix ?? string.Empty;
                      Invalidate();
                  }

                  public void SetConfirmation''',
          '''            currentPrefix = prefix ?? string.Empty;
                      FitToContent();
                      PositionNearCursor();
                      Invalidate();
                  }

                  public void SetConfirmation''')

          replace_once('NX2512_HotkeyStudio/UI/LeaderHudForm.cs',
          '''            searchFilter = null;
                      Invalidate();
                  }

                  public void UpdateTimeoutProgress''',
          '''            searchFilter = null;
                      FitToContent();
                      PositionNearCursor();
                      Invalidate();
                  }

                  public void UpdateTimeoutProgress''')

          replace_once('NX2512_HotkeyStudio/UI/LeaderHudForm.cs',
          '''        private void PositionNearCursor()
                  {''',
          '''        private void FitToContent()
                  {
                      Rectangle working = Screen.PrimaryScreen?.WorkingArea ?? new Rectangle(0, 0, 1280, 720);
                      if (GetCursorPos(out POINT cursor)) working = Screen.FromPoint(new Point(cursor.X, cursor.Y)).WorkingArea;
                      int maxWidth = Math.Max(420, working.Width - 20);
                      int maxHeight = Math.Max(320, working.Height - 20);
                      if (confirmationItem != null)
                      {
                          Size = new Size(Math.Min(900, maxWidth), Math.Min(480, maxHeight));
                          return;
                      }
                      if (searchFilter != null)
                      {
                          int rows = Math.Max(3, Math.Min(12, commands.Count));
                          Size = new Size(Math.Min(960, maxWidth), Math.Min(maxHeight, 166 + rows * 48));
                          return;
                      }

                      int count = Math.Max(1, BuildDisplayRows(commands, currentPrefix).Count);
                      const int rowHeight = 60;
                      const int gutter = 6;
                      const int horizontalPadding = 36;
                      const int verticalPadding = 152;
                      const int minimumCardWidth = 205;
                      int maximumRows = Math.Max(1, (maxHeight - verticalPadding + gutter) / (rowHeight + gutter));
                      int maximumColumns = Math.Max(1, Math.Min(6,
                          (maxWidth - horizontalPadding + gutter) / (minimumCardWidth + gutter)));
                      int columns = Math.Max(1, Math.Min(maximumColumns,
                          (int)Math.Ceiling(count / (double)maximumRows)));
                      int rows = Math.Max(1, (int)Math.Ceiling(count / (double)columns));
                      int width = Math.Min(maxWidth, Math.Max(720,
                          horizontalPadding + columns * minimumCardWidth + (columns - 1) * gutter));
                      int height = Math.Min(maxHeight, Math.Max(320,
                          verticalPadding + rows * (rowHeight + gutter) - gutter));
                      Size = new Size(width, height);
                  }

                  private void PositionNearCursor()
                  {''')

          regex_once('NX2512_HotkeyStudio/UI/LeaderHudForm.cs',
          r'        private void DrawCommandList\(Graphics graphics\)\n        \{.*?\n        \}\n\n        private void DrawCommandRow',
          '''        private void DrawCommandList(Graphics graphics)
                  {
                      using (Font hint = new Font("Segoe UI", 9.5f))
                      using (SolidBrush muted = new SolidBrush(mutedColor))
                          graphics.DrawString($"{triggerKeyName} → клавиша на карточке   ·   Tab модуль   ·   Space поиск", hint, muted, 18, 82);

                      List<DisplayRow> visible = BuildDisplayRows(commands, currentPrefix);
                      if (visible.Count == 0)
                      {
                          using Font emptyFont = new Font("Segoe UI Semibold", 12f);
                          using SolidBrush emptyBrush = new SolidBrush(warningColor);
                          graphics.DrawString("Для этого префикса нет доступных операций.", emptyFont, emptyBrush, 18, 126);
                          return;
                      }

                      const int gutter = 6;
                      const int left = 18;
                      const int top = 108;
                      const int rowHeight = 60;
                      int bottom = Height - 46;
                      int rowsPerColumn = Math.Max(1, (bottom - top + gutter) / (rowHeight + gutter));
                      int columnCount = Math.Max(1, (int)Math.Ceiling(visible.Count / (double)rowsPerColumn));
                      int columnWidth = Math.Max(170, (Width - left * 2 - gutter * (columnCount - 1)) / columnCount);
                      for (int index = 0; index < visible.Count; index++)
                      {
                          int column = index / rowsPerColumn;
                          int row = index % rowsPerColumn;
                          Rectangle rectangle = new Rectangle(left + column * (columnWidth + gutter),
                              top + row * (rowHeight + gutter), columnWidth, rowHeight);
                          DrawCommandRow(graphics, rectangle, visible[index]);
                      }
                  }

                  private void DrawCommandRow''')

          regex_once('NX2512_HotkeyStudio/UI/LeaderHudForm.cs',
          r'        private void DrawSearch\(Graphics graphics\)\n        \{.*?\n        \}\n\n        private List<DisplayRow> BuildDisplayRows',
          '''        private void DrawSearch(Graphics graphics)
                  {
                      using (Font title = new Font("Segoe UI Semibold", 11f))
                      using (SolidBrush text = new SolidBrush(textColor))
                          DrawEllipsized(graphics, $"Поиск в модуле: {searchFilter}   ·   найдено {commands.Count}", title, text,
                              new Rectangle(18, 84, Width - 36, 28));
                      int maximumRows = Math.Max(1, (Height - 174) / 48);
                      int shown = Math.Min(commands.Count, maximumRows);
                      int y = 122;
                      for (int index = 0; index < shown; index++)
                      {
                          LeaderSequenceItem item = commands[index];
                          Rectangle row = new Rectangle(18, y, Width - 36, 42);
                          using (GraphicsPath path = Rounded(row, 10))
                          using (Pen pen = new Pen(Color.FromArgb(64, 83, 100), 1f))
                          {
                              FillPathGradient(graphics, path, row, cardHighlightColor, cardColor, 90f);
                              graphics.DrawPath(pen, path);
                          }
                          Rectangle iconBox = new Rectangle(row.Left + 44, row.Top + 7, 28, 28);
                          OperationThumbnailRenderer.Draw(graphics, iconBox, item.IconHint, item.Command?.ID, item.Command?.Name ?? item.Notes);
                          using (Font rank = new Font("Consolas", 9f, FontStyle.Bold))
                          using (SolidBrush accent = new SolidBrush(accentColor))
                              DrawCentered(graphics, "#" + (index + 1), rank, accent, new Rectangle(row.Left + 7, row.Top + 7, 34, 28));
                          using (Font name = new Font("Segoe UI", 9.5f))
                          using (SolidBrush text = new SolidBrush(textColor))
                              DrawEllipsized(graphics, item.Command?.Name ?? item.Notes, name, text,
                                  new Rectangle(row.Left + 82, row.Top + 3, row.Width - 96, 20));
                          using (Font pathFont = new Font("Consolas", 7.8f))
                          using (SolidBrush muted = new SolidBrush(mutedColor))
                              DrawEllipsized(graphics, item.DisplayPath(triggerKeyName), pathFont, muted,
                                  new Rectangle(row.Left + 82, row.Top + 21, row.Width - 96, 17));
                          y += 48;
                      }
                      using (Font hint = new Font("Segoe UI", 8.5f))
                      using (SolidBrush muted = new SolidBrush(mutedColor))
                      {
                          string overflow = commands.Count > shown ? $" · показано {shown} из {commands.Count}, уточните запрос" : string.Empty;
                          graphics.DrawString("Enter — первый результат · Backspace — удалить символ · Esc — закрыть" + overflow,
                              hint, muted, 18, Height - 52);
                      }
                  }

                  private List<DisplayRow> BuildDisplayRows''')

          replace_once('NX2512_HotkeyStudio/UI/LeaderHudForm.cs',
          '                Name = MenuLabelFor(key, group),',
          '                Name = MenuLabelFor(key, group, prefixDepth),')

          regex_once('NX2512_HotkeyStudio/UI/LeaderHudForm.cs',
          r'        private static string MenuLabelFor\(string key, List<LeaderSequenceItem> group\)\n        \{.*?\n        \}',
          '''        private static string MenuLabelFor(string key, List<LeaderSequenceItem> group, int prefixDepth)
                  {
                      return CommandMenuPolicy.ResolveMenuLabel(key, group, prefixDepth) + " >";
                  }''')

          # Editor preview: scroll through all rows instead of silently clipping them.
          replace_once('NX2512_HotkeyStudio/UI/CommandListPreviewPanel.cs',
          '''            DoubleBuffered = true;
                      BackColor = background;''',
          '''            DoubleBuffered = true;
                      AutoScroll = true;
                      BackColor = background;''')

          replace_once('NX2512_HotkeyStudio/UI/CommandListPreviewPanel.cs',
          '''            commands = Ordered(items);
                      Invalidate();''',
          '''            commands = Ordered(items);
                      UpdateScrollExtent();
                      Invalidate();''')

          replace_once('NX2512_HotkeyStudio/UI/CommandListPreviewPanel.cs',
          '''        protected override void OnPaint(PaintEventArgs e)
                  {''',
          '''        protected override void OnResize(EventArgs eventArgs)
                  {
                      base.OnResize(eventArgs);
                      UpdateScrollExtent();
                  }

                  private void UpdateScrollExtent()
                  {
                      int count = BuildDisplayRows(commands, currentPrefix).Count;
                      int columns = Math.Max(1, Math.Min(3, (Math.Max(360, ClientSize.Width) - 24 + 10) / 220));
                      int rows = Math.Max(1, (int)Math.Ceiling(Math.Max(1, count) / (double)columns));
                      AutoScrollMinSize = new Size(0, 60 + rows * 78 + 12);
                  }

                  protected override void OnPaint(PaintEventArgs e)
                  {''')

          replace_once('NX2512_HotkeyStudio/UI/CommandListPreviewPanel.cs',
          '''            Graphics graphics = e.Graphics;
                      graphics.SmoothingMode = SmoothingMode.AntiAlias;
                      graphics.Clear(background);''',
          '''            Graphics graphics = e.Graphics;
                      graphics.SmoothingMode = SmoothingMode.AntiAlias;
                      graphics.Clear(background);
                      graphics.TranslateTransform(AutoScrollPosition.X, AutoScrollPosition.Y);''')

          regex_once('NX2512_HotkeyStudio/UI/CommandListPreviewPanel.cs',
          r'            int columnCount = 3;\n            int gutter = 10;.*?                DrawRow\(graphics, rectangle, rows\[index\]\);\n            \}',
          '''            const int gutter = 10;
                      const int left = 12;
                      const int top = 60;
                      const int rowHeight = 70;
                      List<DisplayRow> rows = BuildDisplayRows(commands, currentPrefix);
                      int columnCount = Math.Max(1, Math.Min(3, (Math.Max(360, ClientSize.Width) - left * 2 + gutter) / (210 + gutter)));
                      int rowsPerColumn = Math.Max(1, (int)Math.Ceiling(Math.Max(1, rows.Count) / (double)columnCount));
                      int columnWidth = Math.Max(190, (Math.Max(360, ClientSize.Width) - left * 2 - gutter * (columnCount - 1)) / columnCount);
                      for (int index = 0; index < rows.Count; index++)
                      {
                          int column = index / rowsPerColumn;
                          int row = index % rowsPerColumn;
                          var rectangle = new Rectangle(left + column * (columnWidth + gutter), top + row * (rowHeight + 8), columnWidth, rowHeight);
                          DrawRow(graphics, rectangle, rows[index]);
                      }''')

          replace_once('NX2512_HotkeyStudio/UI/CommandListPreviewPanel.cs',
          '''            string label = group.Select(item => item.SubmenuLabel).FirstOrDefault(value => !string.IsNullOrWhiteSpace(value));
                      return new DisplayRow
                      {
                          Key = key,
                          Name = (string.IsNullOrWhiteSpace(label) ? "Подменю " + key : label) + " >",''',
          '''            string label = CommandMenuPolicy.ResolveMenuLabel(key, group, prefixDepth);
                      return new DisplayRow
                      {
                          Key = key,
                          Name = label + " >",''')

          write('NX2512_HotkeyStudio.Tests/NX2512_HotkeyStudio.Tests.csproj', r'''<Project Sdk="Microsoft.NET.Sdk">
            <PropertyGroup>
              <OutputType>Exe</OutputType>
              <TargetFramework>net8.0-windows</TargetFramework>
              <PlatformTarget>x64</PlatformTarget>
              <ImplicitUsings>disable</ImplicitUsings>
              <Nullable>disable</Nullable>
              <LangVersion>latest</LangVersion>
              <Deterministic>true</Deterministic>
            </PropertyGroup>
            <ItemGroup>
              <ProjectReference Include="..\NX2512_HotkeyStudio\NX2512_HotkeyStudio.csproj" />
            </ItemGroup>
          </Project>
          ''')

          write('NX2512_HotkeyStudio.Tests/Program.cs', r'''using System;
          using System.Collections.Generic;
          using System.Linq;
          using NX2512_HotkeyStudio.Models;
          using NX2512_HotkeyStudio.UI;

          internal static class Program
          {
              private static void Main()
              {
                  var first = Command("UG_TEST_FIRST", "First", new[] { "C", "F", "E" });
                  var module = new ModuleConfig
                  {
                      ID = "modeling",
                      Label = "Modeling",
                      LeaderPrefix = "M",
                      CommandSets = new List<ModuleCommandSet>
                      {
                          new ModuleCommandSet { ID = "primary", Commands = new List<ModuleCommand> { first } }
                      }
                  };
                  var config = new Config { Modules = new List<ModuleConfig> { module } };
                  EditableCommandPathPolicy.Normalize(config);
                  Assert(EditableCommandPathPolicy.Validate(config).Count == 0,
                      "Canonical path with empty legacy fields must be valid.");

                  EditableCommandPathPolicy.ApplyEditedPath(first, "E → F → M", "Edit → Feature → Modify");
                  Assert(first.Path.SequenceEqual(new[] { "E", "F", "M" }), "Editor must update canonical Path.");
                  Assert(first.PathLocked && first.PathSource == "user", "Edited paths must be user-owned and locked.");
                  Assert(first.SubmenuKey == "E" && first.InputKey == "M", "Legacy projection must follow canonical Path.");

                  var duplicate = Command("UG_TEST_DUPLICATE", "Duplicate", new[] { "E", "F", "M" });
                  module.CommandSets[0].Commands.Add(duplicate);
                  List<string> duplicateProblems = EditableCommandPathPolicy.Validate(config);
                  Assert(duplicateProblems.Any(value => value.Contains("повторяется", StringComparison.OrdinalIgnoreCase)),
                      "Duplicate canonical paths must be rejected.");

                  duplicate.Path = new List<string> { "E", "F" };
                  List<string> prefixProblems = EditableCommandPathPolicy.Validate(config);
                  Assert(prefixProblems.Any(value => value.Contains("команда/подменю", StringComparison.OrdinalIgnoreCase)),
                      "Terminal-prefix conflicts must be rejected.");

                  var item = new LeaderSequenceItem
                  {
                      PathLabels = new List<string> { "Create", "Feature", "Extrude" },
                      SubmenuLabel = "Legacy root"
                  };
                  Assert(CommandMenuPolicy.ResolveMenuLabel("F", new[] { item }, 2) == "Feature",
                      "Nested menu label must use the matching PathLabels depth.");
                  Console.WriteLine("[OK] Canonical profile editor and nested menu regressions.");
              }

              private static ModuleCommand Command(string id, string name, IEnumerable<string> path) => new ModuleCommand
              {
                  Enabled = true,
                  Path = path.ToList(),
                  Command = new CommandRef { ID = id, Name = name }
              };

              private static void Assert(bool condition, string message)
              {
                  if (!condition) throw new InvalidOperationException(message);
              }
          }
          ''')

          replace_once('.github/workflows/ci.yml',
          '''      - name: Build Hotkey Studio
                  shell: pwsh''',
          '''      - name: Run profile editor and command-menu regressions
                  shell: pwsh
                  run: |
                    dotnet run --project .\NX2512_HotkeyStudio.Tests\NX2512_HotkeyStudio.Tests.csproj -c Release -p:Platform=x64 --nologo *> hotkey-regression-tests.log
                    $code = $LASTEXITCODE
                    Get-Content hotkey-regression-tests.log -Tail 160
                    exit $code

                - name: Build Hotkey Studio
                  shell: pwsh''')

          replace_once('.github/workflows/ci.yml',
          '''            state-machine-tests.log
                      hotkey-build.log''',
          '''            state-machine-tests.log
                      hotkey-regression-tests.log
                      hotkey-build.log''')
          '@ | Set-Content -LiteralPath .\apply-profile-menu-fix.py -Encoding UTF8
          python .\apply-profile-menu-fix.py
          Remove-Item .\apply-profile-menu-fix.py -Force

      - name: Validate generated profiles
        shell: pwsh
        run: |
          node .\scripts\validate-command-tree.mjs
          node .\scripts\validate-main-command-map.mjs

      - name: Run regression tests
        shell: pwsh
        run: dotnet run --project .\NX2512_HotkeyStudio.Tests\NX2512_HotkeyStudio.Tests.csproj -c Release -p:Platform=x64 --nologo

      - name: Build HotkeyStudio
        shell: pwsh
        run: dotnet build .\NX2512_HotkeyStudio\NX2512_HotkeyStudio.csproj -c Release -p:Platform=x64 --nologo

      - name: Commit fixes directly to main
        shell: pwsh
        run: |
          Remove-Item .github/workflows/apply-profile-menu-fix.yml -Force
          git add -A
          git diff --check
          if (git diff --cached --quiet) { throw 'Patch produced no changes.' }
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'Fix canonical profile editing and complete command menus'
          git push origin HEAD:main
